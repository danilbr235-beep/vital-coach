# Backend (Supabase Edge Functions scaffold)

This folder is a **starter scaffold** for Supabase Edge Functions that will host:

- LLM/Coach calls (server-side secrets)
- ingestion jobs
- lab parsing pipeline endpoints

MVP note: the Flutter app currently uses a **local stub** for Coach responses.

