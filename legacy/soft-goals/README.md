# Soft Goals Manager (legacy)

This folder contains the original Node/Express goals demo that previously lived in the repository root.

## Run

```bash
cd legacy/soft-goals
npm install
npm start
```

